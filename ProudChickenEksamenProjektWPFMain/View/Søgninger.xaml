<Window x:Class="ProudChickenEksamenProjektWPFMain.Søgninger"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:ProudChickenEksamenProjektWPFMain"
        mc:Ignorable="d" 
        Title="Søgninger" Height="500" Width="1000"
        
        Background="#FAFAFA">

    <Grid Margin="40,40,40,-6">
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Width="360" Height="Auto">

            <TextBlock Text="Vælg beskedtype:" FontWeight="Bold" Margin="0,0,0,8"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <RadioButton x:Name="Sms" GroupName="BeskedValg" Content="SMS" Margin="0,0,16,0" IsChecked="True"/>
                <RadioButton x:Name="Email" GroupName="BeskedValg" Content="Email"/>
            </StackPanel>

            <ComboBox x:Name="BeskedType"
                      Height="30"
                      Width="200"
                      Loaded="VisBeskedMuligheder"
                      Margin="0,0,0,16" />

            <TextBlock Text="Vælg Søgningstype:" FontWeight="Bold" Margin="0,0,0,8"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <RadioButton x:Name="By" Content="By" GroupName="StedDatoValg" Margin="0,0,27,0" Checked="OpdaterCheckBoxValg"/>
                <RadioButton x:Name="OmrådeNr" Content="OmrådeNr" GroupName="StedDatoValg" Margin="0,0,20,0" Checked="OpdaterCheckBoxValg" />
                <RadioButton x:Name="Dato" Content="Dato" GroupName="StedDatoValg" Margin="0,0,20,0" Checked="OpdaterCheckBoxValg" />
                <RadioButton x:Name="ID" Content="ID" GroupName="StedDatoValg" Margin="0,0,20,0" Checked="OpdaterCheckBoxValg" />
                <RadioButton x:Name="SmsType" Content="SmsType" GroupName="StedDatoValg" Margin="0,0,20,0" Checked="OpdaterCheckBoxValg" />
            </StackPanel>

            <TextBox x:Name="StedValg"
                    Width="200"
                    Height="30"
                    VerticalContentAlignment="Center"
                    Padding="6"
                    Margin="0,0,0,16" >
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsChecked, ElementName=Dato}" Value="True">
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                <DatePicker x:Name="FraDato" Width="140" Margin="0,0,10,0"/>
                <DatePicker x:Name="TilDato" Width="140"/>
            </StackPanel>

            <Button x:Name="visKundeKnappen"
                    Content="Vis kunder"
                    Width="120"
                    Height="35"
                    Background="#0078D7"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="VisKundeListe"
                    Margin="0,0,0,20"
                    BorderThickness="0"
                    Cursor="Hand"
                    IsEnabled="False" />

            <Button Content="Tilbage til Startside"
                    Height="27"
                    Width="140"
                    Background="#0078D7"
                    Foreground="White"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand" 
                    Click="TilbageKnappen"
                    Margin="0,20,0,0" />
            <RadioButton x:Name="SQLSELECT"
                 Content="SQL"
                 GroupName="RepoSelect"
                 Margin="0,0,16,0"
                 IsChecked="True"/>

            <RadioButton x:Name="JSONSELECT"
                 Content="JSON"
                 GroupName="RepoSelect"/>

        </StackPanel>

        <ListBox x:Name="MyListBox"
                 Margin="449,0,0,23"/>



    </Grid>
</Window>
